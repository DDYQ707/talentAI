spring:
  application:
    name: talent-admin
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: talent-ai-dev

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 15000
